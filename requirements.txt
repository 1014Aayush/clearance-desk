# --- Google Cloud / Gemini ---------------------------------------------------
google-genai>=1.28.0
google-adk>=2.6.0
google-cloud-storage>=2.18.0
# Agent Engine deployment only (python -m clearance_desk.deploy). Heavy, and
# not needed to run the service on Cloud Run — install when deploying the agent:
#   pip install "google-cloud-aiplatform[agent_engines,adk]>=1.112.0"

# --- Parallel (partner track) ------------------------------------------------
parallel-web>=0.4.0

# --- Service layer -----------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.12
httpx>=0.27.0
jinja2>=3.1.4

# --- Script / PDF ingest -----------------------------------------------------
pypdf>=5.1.0

# --- Dev ---------------------------------------------------------------------
pytest>=8.3.0
